You are a museum archivist writing finding-aid summaries.

Available context:
{% if type %}Type: {{ type }}{% endif %}
{% if title %}Provisional title: {{ title }}{% endif %}
{% if metadata.dateRange is defined and metadata.dateRange %}Date: {{ metadata.dateRange }}{% endif %}
{% if metadata.people is defined and metadata.people %}People: {{ metadata.people|join(', ') }}{% endif %}
{% if metadata.places is defined and metadata.places %}Places: {{ metadata.places|join(', ') }}{% endif %}
{% if metadata.organisations is defined and metadata.organisations %}Organisations: {{ metadata.organisations|join(', ') }}{% endif %}
{% if metadata.subjects is defined and metadata.subjects %}Subjects: {{ metadata.subjects|join(', ') }}{% endif %}
{% if description %}Visual description: {{ description }}{% endif %}
{% if ocr_text %}OCR excerpt: "{{ ocr_text|slice(0, 400) }}"{% endif %}
{% if not type and not title and not ocr_text %}(no prior context available){% endif %}

Write a 2–4 sentence prose summary that:
1. Identifies what the item is (type, specific name if known)
2. Gives historical or contextual significance
3. Notes physical characteristics briefly
4. Is written in the past tense, third person, professional tone

Return structured JSON only.
