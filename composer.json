{
    "name": "survos/ai-pipeline-bundle",
    "description": "Symfony bundle for resumable, ordered AI task pipelines — OCR, classify, describe, extract, summarize, and more, out of the box.",
    "type": "symfony-bundle",
    "license": "MIT",
    "keywords": ["symfony", "ai", "pipeline", "ocr", "vision", "llm", "openai", "mistral"],
    "autoload": {
        "psr-4": {
            "Survos\\AiPipelineBundle\\": "src/"
        }
    },
    "require": {
        "php": ">=8.2",
        "symfony/framework-bundle": "^7.0|^8.0",
        "symfony/http-client": "^7.0|^8.0",
        "symfony/twig-bundle": "^7.0|^8.0",
        "twig/twig": "^3.0",
        "symfony/ai-bundle": "^0.5",
        "symfony/ai-agent": "^0.5",
        "symfony/ai-open-ai-platform": "^0.5",
        "symfony/ai-mistral-platform": "^0.5"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0|^12.0|^13.0"
    },
    "extra": {
        "symfony": {
            "require": "^7.0|^8.0"
        }
    }
}
